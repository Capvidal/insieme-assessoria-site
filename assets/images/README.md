# Imagens do site

Coloque aqui as imagens institucionais do site. Nomes sugeridos (usados no código):

- `hero.jpg` — imagem principal da seção "Início" (já incluída; foto de equipe trabalhando com
  notebooks/tablets, banco gratuito [Pexels](https://www.pexels.com/photo/3183197/), uso livre
  sem atribuição obrigatória). Substitua por outra a qualquer momento, mantendo o mesmo nome
  de arquivo ou ajustando o `src` em `index.html`.
- `og-cover.jpg` — já incluída (1200x630px), gerada a partir da identidade visual da Insieme
  (marca + nome + tagline sobre fundo navy). Substitua por uma arte definitiva quando desejar,
  mantendo o mesmo nome de arquivo e dimensões.
- `logo.svg` / `logo-header.svg` — marca da Insieme Assessoria (versões clara e escura).
- `hero.svg` — ilustração alternativa (fallback), caso prefira não usar fotografia.
